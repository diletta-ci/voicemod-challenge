# Voicemod - Dev Challenge

## Dev - Running Project

In project root open terminal and run:

```powershell
npm i

#For development mode, run
npm run dev

#For production mode, run
npm run build

```

After build, production files will be available in app/dist/
